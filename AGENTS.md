# AGENTS.md — CentrixCore (`compritas-core`)

> **Purpose:** This document is intended for AI agents and developers who need to understand this codebase quickly. It describes the project's purpose, architecture, domain terminology, data models, and conventions.
>
> **Per-document implementation guides** (precise code-level references for each SRI document type) are in the [`docs/`](./docs/) directory:
> - [`docs/facturas.md`](./docs/facturas.md) — Factura (Invoice, `cod_doc = 01`)
> - [`docs/notas_de_credito.md`](./docs/notas_de_credito.md) — Nota de Crédito (Credit Note, `cod_doc = 04`)
> - [`docs/xbes.md`](./docs/xbes.md) — XAdES-BES digital signing implementation

---

## 1. What Is This Project?

`centrix_core` (also referred to as `CentrixCore`) is an **Elixir library** that handles the full lifecycle of **electronic invoicing (facturación electrónica)** in **Ecuador** as mandated by the **SRI** (*Servicio de Rentas Internas* — Ecuador's Internal Revenue Service).

It is **not a web application**. It is a **pure Elixir library** meant to be consumed by other Elixir/Phoenix applications (e.g., `compritas` or `compritas-saas`) via a `mix.exs` dependency.

### Core Capabilities

| Capability | Status |
|---|---|
| XML generation for invoices (`factura`) | ✅ Implemented |
| XML generation for credit notes (`nota de crédito`) | ✅ Implemented |
| XML digital signing (XAdES-BES) | ✅ Implemented |
| Submission to SRI SOAP web services | ✅ Implemented |
| Authorization polling from SRI | ✅ Implemented |
| XML parsing of authorized invoices | ✅ Implemented |
| PDF generation of invoices | ✅ Implemented |
| Barcode (Code128) generation | ✅ Implemented |
| Credit notes (notas de débito), retenciones, guías de remisión | 🔜 Planned |

---

## 2. Ecuador Electronic Invoicing Domain

Understanding this domain is **critical** to working with this codebase.

### SRI (Servicio de Rentas Internas)
The government tax authority of Ecuador. All businesses must submit electronic documents (invoices, credit notes, etc.) to SRI for validation and authorization. The process is **offline** — documents are submitted to a web service and then queried for authorization status.

### Document Types (Comprobantes Electrónicos)

| Spanish Name | English | `cod_doc` |
|---|---|---|
| Factura | Invoice | `01` |
| Nota de Crédito | Credit Note | `04` |
| Nota de Débito | Debit Note | `05` |
| Guía de Remisión | Remittance Guide | `06` |
| Comprobante de Retención | Withholding Voucher | `07` |

### Key Domain Terms

| Term | Meaning |
|---|---|
| `clave_acceso` | 49-digit unique access key identifying a document. Generated by the library using the Módulo 11 algorithm. |
| `digito_verificador` | Check digit (Módulo 11) appended to the `clave_acceso`. |
| `RUC` | *Registro Único de Contribuyentes* — Ecuador's business tax ID (13 digits). |
| `ambiente` | Environment: `1` = Testing (`PRUEBAS`), `2` = Production (`PRODUCCION`). |
| `tipo_emision` | Emission type: `1` = Normal. |
| `tipo_comprobante` | Numeric code identifying the document type (see table above). |
| `estab` | Establishment number (3 digits, zero-padded). |
| `pto_emi` | Emission point (3 digits, zero-padded). |
| `secuencial` | Document sequence number (9 digits, zero-padded). |
| `razon_social` | Legal company name. |
| `nombre_comercial` | Trade name. |
| `dir_matriz` | Main office address. |
| `obligado_contabilidad` | Whether the company is obligated to keep accounting records: `"SI"` or `"NO"`. |
| `contribuyente_especial` | Special taxpayer registration number (optional). |
| `RISE` | *Régimen Impositivo Simplificado Ecuatoriano* — simplified tax regime code. |
| `IVA` | VAT (*Impuesto al Valor Agregado*). Common rates: 0%, 12%, 13%, 14%, 15%. |
| `infoTributaria` | XML block with the issuer's tax/legal data. |
| `infoFactura` | XML block with the invoice transactional data (buyer, totals, payments). |
| `detalles` | Line items on the invoice. |
| `infoAdicional` | Free-form key-value extra fields on the document. |
| `motivo` | Reason field on a credit note. |
| `cod_doc_modificado` | Code of the original document being corrected by a credit note. |
| `num_doc_modificado` | Number of the original document being corrected. |
| `XAdES-BES` | XML Advanced Electronic Signature — Basic Electronic Signature. The digital signature standard required by SRI. |
| `P12 / PKCS#12` | Certificate file format (`.p12`) used for digital signing. Issued by authorized CAs in Ecuador (e.g., BCE, Security Data). |

### IVA Tax Codes (`codigo_porcentaje`)

| Code | Label |
|---|---|
| `0` | IVA 0% |
| `2` | IVA 12% |
| `3` | IVA 14% |
| `4` | IVA 15% |
| `6` | No objeto de impuesto |
| `7` | Exento de IVA |
| `10` | IVA 13% |

### Payment Methods (`forma_pago`)

| Code | Method |
|---|---|
| `01` | SIN UTILIZACION DEL SISTEMA FINANCIERO |
| `15` | COMPENSACIÓN DE DEUDAS |
| `16` | TARJETA DE DÉBITO |
| `17` | DINERO ELECTRÓNICO |
| `18` | TARJETA PREPAGO |
| `19` | TARJETA DE CRÉDITO |
| `20` | OTROS CON UTILIZACION DEL SISTEMA FINANCIERO |
| `21` | ENDOSO DE TÍTULOS |

---

## 3. Technology Stack

| Technology | Role |
|---|---|
| Elixir `~> 1.17` | Primary language |
| `ecto_sql ~> 3.13` | Embedded schemas & changesets for data validation (no DB) |
| `xml_builder ~> 2.4` | Generates XML document trees |
| `httpoison ~> 2.2` | HTTP client for SRI SOAP web service calls |
| `timex ~> 3.7` | Timezone-aware datetime formatting (signing timestamp) |
| `xmerl_c14n ~> 0.2` | XML canonicalization (C14N) required for XAdES signatures |
| `sweet_xml ~> 0.7` | XPath-based XML parsing |
| `elixir_xml_to_map ~> 3.1` | Converts XML strings to Elixir maps |
| `poison ~> 6.0` | JSON encoding (auxiliary use) |
| `pdf ~> 0.7` | PDF generation |
| `barlix ~> 0.6` | Code128 barcode PNG generation |
| `mimic ~> 2.0` (test only) | Mocking library for tests |
| `openssl` (system) | Used via `System.cmd/2` to read P12 certificates |

---

## 4. Project Structure

```
compritas-core/
├── lib/
│   ├── centrix_core.ex              # Root module: global config (URLs, timeouts, timezone)
│   ├── barcode_generator.ex         # Code128 barcode PNG generator
│   ├── invoice_pdf_builder.ex       # PDF invoice renderer
│   ├── invoice_xml_parser.ex        # Parses authorized invoice XML → structured map
│   ├── signing.ex                   # Public signing entry point
│   ├── sri_client.ex                # Public SRI communication entry point
│   ├── xml_builder.ex               # Public invoice XML builder entry point
│   ├── xml_credit_note_builder.ex   # Public credit note XML builder entry point
│   │
│   ├── signing/
│   │   ├── p12_reader.ex            # Reads P12 cert → PEM cert + RSA key via openssl CLI
│   │   └── xbes/
│   │       ├── cfg.ex               # Signature configuration struct
│   │       ├── signature.ex         # Assembles the <ds:Signature> XML block
│   │       ├── signed_info.ex       # Builds <ds:SignedInfo> with digest references
│   │       ├── util.ex              # Helpers (XML attributes, canonicalization)
│   │       ├── xbes.ex              # Orchestrates XAdES-BES signing flow
│   │       └── signed_info/
│   │           ├── doc.ex           # Digest of the document to be signed
│   │           ├── key_info.ex      # Builds <ds:KeyInfo> (certificate data)
│   │           └── properties.ex    # Builds <xades:SignedProperties> block
│   │
│   ├── sri_client/
│   │   ├── parsers/
│   │   │   ├── authorization_parser.ex  # Parses SRI authorization SOAP response
│   │   │   └── reception_parser.ex      # Parses SRI reception SOAP response
│   │   └── ws/
│   │       ├── client.ex                # HTTPoison-based SOAP HTTP client
│   │       ├── client_behaviour.ex      # Behaviour contract for the client
│   │       ├── authorization_soap.ex    # Builds autorizacionComprobante SOAP envelope
│   │       └── reception_soap.ex        # Builds validarComprobante SOAP envelope
│   │
│   └── xml_builder/
│       └── dataset/
│           ├── changeset_helper.ex     # Shared changeset utilities
│           ├── clave_acceso.ex         # Access key embedded schema
│           ├── clave_acceso/
│           │   └── digito_verificador.ex  # Módulo 11 check-digit algorithm
│           ├── comprobante.ex          # Base comprobante schema
│           ├── factura.ex              # Top-level Factura schema + XML serialization
│           ├── factura/
│           │   ├── campo_adicional.ex  # <campoAdicional> schema + XML
│           │   ├── det_adicional.ex    # <detAdicional> schema + XML
│           │   ├── detalle.ex          # <detalle> (line item) schema + XML
│           │   ├── impuesto.ex         # <impuesto> (line-level tax) schema + XML
│           │   ├── info_factura.ex     # <infoFactura> block schema + XML
│           │   ├── info_tributaria.ex  # <infoTributaria> block schema + XML (also computes clave_acceso)
│           │   ├── pago.ex             # <pago> (payment) schema + XML
│           │   └── total_impuesto.ex   # <totalImpuesto> (totals) schema + XML
│           ├── nota_credito.ex         # Top-level NotaCredito schema + XML serialization
│           └── nota_credito/
│               ├── campo_adicional.ex
│               ├── det_adicional.ex
│               ├── detalle.ex
│               ├── impuesto.ex
│               ├── info_nota_credito.ex  # <infoNotaCredito> block schema + XML
│               ├── info_tributaria.ex
│               └── total_impuesto.ex
│
├── sandbox/
│   ├── invoice_sandbox.ex       # End-to-end invoice test against SRI test environment
│   ├── credit_note_sandbox.ex   # End-to-end credit note test
│   ├── test_invoice.exs         # Entry point: mix run sandbox/test_invoice.exs
│   └── test_credit_note.exs     # Entry point: mix run sandbox/test_credit_note.exs
│
├── example/
│   ├── adapter.ex               # Example adapter showing how a host app can call the library
│   └── invoice.exs              # Example invoice data script
│
├── test/
│   ├── centrix_core_test.exs
│   ├── signing_test.exs
│   ├── sri_client_test.exs
│   ├── xml_builder_test.exs
│   ├── xml_credit_note_builder_test.exs
│   ├── invoice_pdf_builder_test.exs
│   ├── invoice_xml_parser_test.exs
│   └── fixtures/                # Test XML files, P12 cert, PEM keys, expected XML outputs
│
├── mix.exs                      # Project definition and dependencies
├── mix.lock
└── README.md
```

---

## 5. Document-Specific Implementation Guides

For detailed, code-level documentation on each implemented SRI document type — including full schema field references, XML output structure, minimal input examples, and conventions — see the `docs/` directory:

| Document | `cod_doc` | Guide |
|---|---|---|
| Factura (Invoice) | `01` | [`docs/facturas.md`](./docs/facturas.md) |
| Nota de Crédito (Credit Note) | `04` | [`docs/notas_de_credito.md`](./docs/notas_de_credito.md) |
| Nota de Débito | `05` | 🔜 Not yet implemented |
| Guía de Remisión | `06` | 🔜 Not yet implemented |
| Comprobante de Retención | `07` | 🔜 Not yet implemented |

**Cross-cutting guides:**

| Topic | Guide |
|---|---|
| XAdES-BES digital signing | [`docs/xbes.md`](./docs/xbes.md) |

> When implementing a new document type, read [`docs/notas_de_credito.md § 8`](./docs/notas_de_credito.md#8-guidance-for-implementing-future-document-types) for a step-by-step checklist.

---

## 6. Key Modules & Their Roles

### `CentrixCore` (`lib/centrix_core.ex`)
The root configuration module. Provides global constants:
- **SRI SOAP URLs** for test (`celcer.sri.gob.ec`) and production (`cel.sri.gob.ec`) environments.
- Timeout values (all set to 900,000 ms / 15 minutes — SRI can be slow).
- Timezone: `"America/Guayaquil"` (Ecuador standard time, no DST).

### `CentrixCore.XmlBuilder` (`lib/xml_builder.ex`)
Public entry point for building a **factura** XML. Accepts a params map, validates it through Ecto changesets, and returns `{:ok, [xml: xml_string, clave_acceso: string]}` or `{:error, changeset}`.

### `CentrixCore.XmlCreditNoteBuilder` (`lib/xml_credit_note_builder.ex`)
Same pattern as `XmlBuilder` but for **notas de crédito**.

### `CentrixCore.Signing` (`lib/signing.ex`)
Public signing entry point. Reads the P12 certificate, extracts PEM cert and RSA key, then delegates to `CentrixCore.Xbes.sign/5`. Returns `{:ok, signed_xml_string}` or `{:error, reason}`.

### `CentrixCore.SriClient` (`lib/sri_client.ex`)
Handles communication with SRI:
- `send_document(xml, environment)` — submits XML via `validarComprobante` SOAP operation.
- `is_authorized(clave_acceso, environment)` — polls `autorizacionComprobante` SOAP operation.
- Environment `1` → test URLs, `2` → production URLs.

### `CentrixCore.InvoiceXmlParser` (`lib/invoice_xml_parser.ex`)
Parses the full SOAP authorization response XML into a structured Elixir map. Handles single and multiple line items, optional fields, payment methods, and tax labels. Used as input to the PDF builder.

### `CentrixCore.InvoicePdfBuilder` (`lib/invoice_pdf_builder.ex`)
Renders a **PDF** invoice (A4) from the parsed XML map. Supports:
- Company logo image (optional).
- Code128 barcode image (optional).
- Multi-page layout with repeated headers.
- Additional info fields (`infoAdicional`).
- Payment details and totals table.

### `CentrixCore.BarcodeGenerator` (`lib/barcode_generator.ex`)
Generates a Code128 barcode PNG from any string (typically the `clave_acceso`). Saves to a temp file and returns `{:ok, path}`.

### `CentrixCore.P12Reader` (`lib/signing/p12_reader.ex`)
Calls `openssl pkcs12` via `System.cmd/2` to extract PEM certificate and RSA private key. Automatically adds the `-legacy` flag for OpenSSL 3.x compatibility (important: many Ecuadorian P12 certs use legacy encryption algorithms).

### `CentrixCore.Xbes` (`lib/signing/xbes/xbes.ex`)
Implements the **XAdES-BES** digital signature standard:
1. Builds `Properties` XML block and computes its SHA1 digest.
2. Builds `KeyInfo` XML block (certificate data) and computes its SHA1 digest.
3. Computes SHA1 digest of the original XML document.
4. Builds `SignedInfo` referencing all three digests.
5. Signs `SignedInfo` with the RSA private key (SHA1withRSA).
6. Assembles the `<ds:Signature>` block and injects it into the original XML.

---

## 7. The Full Electronic Invoicing Lifecycle

This is the end-to-end flow that all host applications must follow:

```
1. BUILD XML
   CentrixCore.XmlBuilder.build_invoice(params)
   → {:ok, [xml: xml_string, clave_acceso: "49-digit-key"]}

2. SIGN XML
   CentrixCore.Signing.sign(xml_string, p12_path, p12_password)
   → {:ok, signed_xml_string}

3. SEND TO SRI (Reception)
   CentrixCore.SriClient.send_document(signed_xml, environment)
   → {:ok, %{status: "RECIBIDA", response: soap_response}}
   (status "DEVUELTA" = rejected with errors)

4. POLL FOR AUTHORIZATION
   CentrixCore.SriClient.is_authorized(clave_acceso, environment)
   → {:ok, %{status: "AUTORIZADO", response: full_soap_xml}}
   (status "NO AUTORIZADO" or "NO ENCONTRADO O PENDIENTE")

5. (OPTIONAL) PARSE AUTHORIZED XML
   CentrixCore.InvoiceXmlParser.parse_xml(full_soap_xml)
   → structured map with document fields

6. (OPTIONAL) GENERATE BARCODE
   CentrixCore.BarcodeGenerator.generate(clave_acceso)
   → {:ok, "/tmp/barcode_xxx.png"}

7. (OPTIONAL) GENERATE PDF
   CentrixCore.InvoicePdfBuilder.build(parsed_xml_map, logo_path, barcode_path)
   → binary PDF content
```

---

## 8. Data Schemas (Ecto Embedded Schemas)

All schemas use **Ecto embedded schemas** (no database). They serve purely for validation and structured data handling.

### Factura (Invoice)
```
Factura
├── info_tributaria (InfoTributaria)
│   ├── ambiente: integer (1=test, 2=prod)
│   ├── tipo_emision: integer (1=normal)
│   ├── razon_social: string
│   ├── nombre_comercial: string
│   ├── ruc: string (13 chars)
│   ├── cod_doc: integer (1 for factura)
│   ├── estab: integer (3 digits)
│   ├── pto_emi: integer (3 digits)
│   ├── secuencial: integer (9 digits)
│   ├── dir_matriz: string
│   └── clave (ClaveAcceso) — used to compute clave_acceso via Módulo 11
│       ├── fecha_emision: date
│       ├── tipo_comprobante: integer
│       ├── ruc: string
│       ├── ambiente: integer
│       ├── pto_emi: integer
│       ├── estab: integer
│       ├── secuencial: integer
│       ├── codigo: integer (random 8-digit code)
│       └── tipo_emision: integer
│
├── info_factura (InfoFactura)
│   ├── fecha_emision: date
│   ├── dir_establecimiento: string
│   ├── obligado_contabilidad: string ("SI" | "NO")
│   ├── contribuyente_especial: string (optional)
│   ├── tipo_identificacion_comprador: integer
│   ├── razon_social_comprador: string
│   ├── identificacion_comprador: string
│   ├── direccion_comprador: string (optional)
│   ├── total_sin_impuestos: float
│   ├── total_descuento: float
│   ├── propina: float
│   ├── importe_total: float
│   ├── moneda: string (e.g., "DOLAR")
│   ├── total_con_impuestos: [TotalImpuesto]
│   └── pagos: [Pago]
│
├── detalles: [Detalle]
│   └── each Detalle:
│       ├── codigo_principal: string (required)
│       ├── codigo_auxiliar: string (optional)
│       ├── descripcion: string
│       ├── cantidad: float (6 decimal places in XML)
│       ├── precio_unitario: float (6 decimal places)
│       ├── descuento: float
│       ├── precio_total_sin_impuesto: float
│       ├── detalles_adicionales: [DetAdicional]
│       └── impuestos: [Impuesto]
│
└── info_adicional: [CampoAdicional]
    └── each: nombre: string, valor: string
```

### NotaCredito (Credit Note)
Same structure as `Factura` except:
- Uses `info_nota_credito` instead of `info_factura`.
- `info_nota_credito` includes: `cod_doc_modificado`, `num_doc_modificado`, `fecha_emision_doc_sustento`, `valor_modificacion`, `motivo`, `rise` (optional).
- No `pagos` field.

---

## 9. Clave de Acceso Algorithm

The `clave_acceso` is a 49-character string computed as:

```
[fecha_emision(8)][tipo_comprobante(2)][ruc(13)][ambiente(1)][serie(6)][secuencial(9)][codigo(8)][tipo_emision(1)][digito_verificador(1)]
```

- `serie` = `estab(3) + pto_emi(3)`
- `digito_verificador` = Módulo 11 of the first 48 digits, with special cases: result 10 → `1`, result 11 → `0`

Implemented in `CentrixCore.Dataset.ClaveAcceso.DigitoVerificador`.

---

## 10. SRI Web Services

### Endpoints

| Environment | Service | URL |
|---|---|---|
| Test | Reception | `https://celcer.sri.gob.ec/comprobantes-electronicos-ws/RecepcionComprobantesOffline?wsdl` |
| Test | Authorization | `https://celcer.sri.gob.ec/comprobantes-electronicos-ws/AutorizacionComprobantesOffline?wsdl` |
| Production | Reception | `https://cel.sri.gob.ec/comprobantes-electronicos-ws/RecepcionComprobantesOffline?wsdl` |
| Production | Authorization | `https://cel.sri.gob.ec/comprobantes-electronicos-ws/AutorizacionComprobantesOffline?wsdl` |

### SOAP Operations
- **Reception:** `validarComprobante` — sends the signed XML (Base64-encoded inside the SOAP envelope).
- **Authorization:** `autorizacionComprobante` — queries by `clave_acceso`.

### Reception Statuses
- `RECIBIDA` — document received and queued.
- `DEVUELTA` — document rejected (check SOAP response for error details).

### Authorization Statuses
- `AUTORIZADO` — document is authorized; the response contains the full authorized XML.
- `NO AUTORIZADO` — document was rejected by SRI.
- `NO ENCONTRADO O PENDIENTE` — not found or still processing (poll again).

### HTTP Notes
- Requests use `Content-Encoding: gzip` and `Accept-Encoding: gzip,deflate`.
- The response body is decompressed transparently by `CentrixCore.Ws.Client`.
- Timeouts are set to **900 seconds** to account for SRI latency.

---

## 11. XAdES-BES Digital Signing Details

The signing process (`CentrixCore.Xbes`) follows the XAdES-BES standard as specified by SRI:

1. **Certificate reading:** P12 → PEM cert + RSA key via `openssl pkcs12`. The `-legacy` flag is automatically added for OpenSSL ≥ 3.0.
2. **Canonicalization:** `http://www.w3.org/TR/2001/REC-xml-c14n-20010315` (C14N exclusive without comments).
3. **Digest algorithm:** SHA1 (`http://www.w3.org/2000/09/xmldsig#sha1`).
4. **Signature algorithm:** RSA-SHA1 (`http://www.w3.org/2000/09/xmldsig#rsa-sha1`).
5. **Signature injection:** The `<ds:Signature>` block is inserted just before the closing tag of the root XML element using a regex replace.
6. **Random IDs:** All XAdES element IDs use random 6-digit numbers to ensure uniqueness per signing.

---

## 12. Adding This Library as a Dependency

```elixir
# In mix.exs of the host application
defp deps do
  [
    {:centrix_core, github: "joselo/centrix-core", branch: "master"}
  ]
end
```

The library is **not published to Hex.pm**. It must be referenced directly from GitHub.

---

## 13. Running Sandbox Tests

The `sandbox/` directory contains end-to-end tests that run against SRI's test environment. These require a valid `.p12` certificate file and its password.

```bash
# Run full invoice flow against SRI test environment
TEST_P12_FILE_PASSWORD="your_password" mix run sandbox/test_invoice.exs

# Run full credit note flow against SRI test environment
TEST_P12_FILE_PASSWORD="your_password" mix run sandbox/test_credit_note.exs
```

The sandbox uses `test/fixtures/file.p12` as the certificate.

---

## 14. Running Tests

```bash
mix test
```

Tests use `mimic` for mocking. Key test files:
- `test/centrix_core_test.exs` — integration-style tests for the full invoice flow.
- `test/xml_builder_test.exs` — validates XML output structure for invoices.
- `test/xml_credit_note_builder_test.exs` — validates XML output for credit notes.
- `test/signing_test.exs` — tests the digital signing flow.
- `test/sri_client_test.exs` — tests SRI SOAP communication with mocked HTTP responses.
- `test/invoice_xml_parser_test.exs` — tests parsing of authorized invoice XML.
- `test/invoice_pdf_builder_test.exs` — tests PDF generation.

Test fixtures in `test/fixtures/` include sample XML documents, pre-signed XMLs, SRI SOAP responses, and a test P12 certificate.

---

## 15. Important Conventions & Gotchas

1. **No database.** Ecto is used solely for embedded schema validation. There are no migrations, no Repo, no database.

2. **Decimal precision.** Monetary values use **2 decimal places** (defined in `CentrixCore.decimals/0`). Quantities and unit prices use **6 decimal places** in XML output.

3. **Dates in XML.** SRI requires dates in `dd/mm/yyyy` format (not ISO 8601). The `format_fecha_emision/1` helper converts `Date` structs accordingly.

4. **Numeric padding.** `cod_doc` → 2 digits, `estab`/`pto_emi` → 3 digits, `secuencial` → 9 digits (all zero-padded).

5. **OpenSSL legacy flag.** Many Ecuadorian P12 certificates use legacy encryption (`RC2-40-CBC`). OpenSSL 3+ requires the `-legacy` flag, which is added automatically by `P12Reader`.

6. **SRI timezone.** All signing timestamps use `America/Guayaquil` (UTC-5, no DST). This is hardcoded in `CentrixCore.timezone/0`.

7. **`contribuyente_especial` is conditional.** The `<contribuyenteEspecial>` XML element is only inserted when `obligado_contabilidad` is `"SI"`.

8. **`codigo_auxiliar` is optional.** In `Detalle`, this field is only included in the XML when it has a non-nil value.

9. **Credit note references the original document.** A `NotaCredito` must include `cod_doc_modificado` (e.g., `"01"` for a factura) and `num_doc_modificado` (the full `estab-pto_emi-secuencial` number of the original).

10. **SOAP responses are gzip-compressed.** The `Ws.Client` module transparently handles decompression based on the `Content-Encoding` header.

11. **Signing injects into the last tag.** The `Xbes.merge/2` function uses a regex (`~r/(<[^<]+)$/) to find the last XML opening tag and inserts the signature block before it.

---

## 16. Example Usage (from `example/adapter.ex`)

```elixir
# Step 1: Build invoice XML
{:ok, [xml: xml, clave_acceso: key]} =
  CentrixCore.XmlBuilder.build_invoice(invoice_params)

# Step 2: Sign the XML
{:ok, signed_xml} =
  CentrixCore.Signing.sign(xml, "/path/to/cert.p12", "p12_password")

# Step 3: Send to SRI
{:ok, %{status: "RECIBIDA", response: resp}} =
  CentrixCore.SriClient.send_document(signed_xml, 1)

# Step 4: Check authorization
{:ok, %{status: "AUTORIZADO", response: auth_resp}} =
  CentrixCore.SriClient.is_authorized(key, 1)

# Step 5: Generate PDF
parsed = CentrixCore.InvoiceXmlParser.parse_xml(auth_resp)
pdf_binary = CentrixCore.InvoicePdfBuilder.build(parsed, logo_path, barcode_path)
```

---

## 17. Project Status & Roadmap

- **Version:** `0.1.0` (active development, API may change before 1.0.0)
- **Elixir requirement:** `~> 1.17`
- **License:** MIT

### Planned additions
- Notas de débito
- Retenciones (withholding vouchers)
- Guías de remisión
- Comprobantes de retención
